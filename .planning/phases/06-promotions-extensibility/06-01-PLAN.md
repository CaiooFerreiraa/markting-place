---
phase: 06-promotions-extensibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/services/coupon-service.ts, src/app/api/checkout/validate-coupon/route.ts, src/components/checkout/coupon-input.tsx]
autonomous: true
requirements: [PROM-01, PROM-03]
must_haves:
  truths:
    - "Buyer can enter a coupon code in checkout and see the discount applied"
    - "System prevents using expired or non-existent coupon codes"
    - "Order subtotal is correctly reduced by fixed or percentage discounts"
  artifacts:
    - path: "src/lib/services/coupon-service.ts"
      provides: "Coupon validation logic"
    - path: "src/app/api/checkout/validate-coupon/route.ts"
      provides: "Coupon validation endpoint"
  key_links:
    - from: "src/components/checkout/coupon-input.tsx"
      to: "/api/checkout/validate-coupon"
      via: "fetch"
---

<objective>
Implement the core Coupon validation and application logic.
Purpose: Allow sellers to offer discounts and buyers to redeem them.
Output: Working coupon input in checkout with server-side validation.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-promotions-extensibility/06-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Coupon Validation Service</name>
  <files>src/lib/services/coupon-service.ts</files>
  <action>Create a `CouponService` class with a `validateCoupon(code: string, storeId: string, orderAmount: number)` method. Check for existence, store matching, expiry date, usage limit, and minimum order amount. Return a standard object with success boolean, discount details, and error message if invalid.</action>
  <verify>Check file exists and has correct validation logic for fixed and percentage discounts.</verify>
  <done>Coupon validation logic is correctly implemented according to schema.</done>
</task>

<task type="auto">
  <name>task 2: Implement Coupon Validation API</name>
  <files>src/app/api/checkout/validate-coupon/route.ts</files>
  <action>Create POST endpoint that accepts {code, storeId, amount}, uses `CouponService`, and returns JSON response with validation results. Handle Prisma queries to find the coupon by code.</action>
  <verify>POST request to /api/checkout/validate-coupon returns correct discount data or error.</verify>
  <done>API endpoint is functional and secure.</done>
</task>

<task type="auto">
  <name>task 3: Add Coupon Input to Checkout UI</name>
  <files>src/components/checkout/coupon-input.tsx, src/app/(shop)/checkout/page.tsx</files>
  <action>Create `CouponInput` component using shadcn Input and Button. Integrate it into the Checkout page. Update the checkout state to display the discounted total when a valid coupon is applied.</action>
  <verify>Manual check: enter "INVALID" -> see error; enter valid coupon (create dummy in DB if needed) -> see subtotal update.</verify>
  <done>UI correctly handles coupon entry and updates totals.</done>
</task>

</tasks>

<verification>
Ensure a coupon correctly reduces the total amount of an order in the checkout summary.
</verification>

<success_criteria>
Buyer can apply a store-specific coupon and see a visual update in the order total.
</success_criteria>

<output>
After completion, create .planning/phases/06-promotions-extensibility/06-01-SUMMARY.md
</output>
