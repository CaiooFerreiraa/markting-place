---
phase: 07-verification-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/07-verification-refinement/E2E-CHECKLIST.md", "src/lib/plugins/index.ts", "src/lib/plugins/notifications.ts"]
autonomous: false
requirements: [VERIF-01, PLUG-01]
user_setup: []

must_haves:
  truths:
    - "Seller receives an email/log when an order is paid"
    - "Full checkout flow is documented and verifiable"
  artifacts:
    - path: "src/lib/plugins/notifications.ts"
      provides: "Real notification logic"
    - path: ".planning/phases/07-verification-refinement/E2E-CHECKLIST.md"
      provides: "Step-by-step verification guide"
  key_links:
    - from: "src/lib/plugins/index.ts"
      to: "src/lib/plugins/notifications.ts"
      via: "import and registration"
---

<objective>
Verify the end-to-end flow of the marketplace and implement a functional notification plugin.

Purpose: Ensure the system works as expected for users and sellers.
Output: E2E Checklist and Notification Plugin implementation.
</objective>

<execution_context>
@~\.config\opencode\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/plugins/index.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create E2E Verification Checklist</name>
  <files>.planning/phases/07-verification-refinement/E2E-CHECKLIST.md</files>
  <action>
    Create a comprehensive markdown checklist for manual E2E testing. 
    Include steps for:
    - Seller Registration
    - Store Creation (Verify Stripe ID in DB)
    - Product Creation
    - Buyer Cart Management (Multi-vendor)
    - Coupon Application (Validation)
    - Stripe Checkout (Simulation)
    - Payment Split Verification
    - Order Status Update & Plugin Trigger
  </action>
  <verify>Check if file exists and contains all required steps</verify>
  <done>E2E-CHECKLIST.md is created with all verification steps</done>
</task>

<task type="auto">
  <name>task 2: Implement Real Notification Plugin</name>
  <files>src/lib/plugins/notifications.ts, src/lib/plugins/index.ts</files>
  <action>
    - Create `src/lib/plugins/notifications.ts` with a `sendSellerNotification` function.
    - For now, implement it as a robust logging system that mimics an email send (or uses Resend if API key is present in env).
    - Update `src/lib/plugins/index.ts` to use this real implementation instead of the inline mock.
    - Ensure it pulls the seller's email from the database based on the `orderId`.
  </action>
  <verify>Run a mock event trigger and check logs/output</verify>
  <done>Notification plugin is implemented and registered</done>
</task>

<task type="checkpoint:human-verify">
  <name>task 3: Perform E2E Manual Walkthrough</name>
  <what-built>Full marketplace flow and notification system</what-built>
  <how-to-verify>
    Follow the steps in `.planning/phases/07-verification-refinement/E2E-CHECKLIST.md`.
    Specifically verify that:
    1. A store created via the wizard has a `stripeAccountId` in the `User` table.
    2. Applying a coupon correctly reduces the total in the checkout UI.
    3. Completing a checkout (even in test mode) triggers the "order.paid" event.
    4. The notification log shows the correct seller email and order details.
  </how-to-verify>
  <resume-signal>Type "verified" if the flow works or list the issues found.</resume-signal>
</task>

</tasks>

<verification>
Check for "order.paid" event logs in the console during checkout.
Verify database records for orders and sub-orders after checkout.
</verification>

<success_criteria>
E2E Checklist completed with no blockers.
Notification plugin successfully logs/sends alerts on payment.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-refinement/07-01-SUMMARY.md`
</output>
