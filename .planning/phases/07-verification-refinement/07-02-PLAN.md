---
phase: 07-verification-refinement
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["src/app/(shop)/checkout/page.tsx", "src/components/store/store-wizard.tsx", "src/components/ui/error-boundary.tsx"]
autonomous: true
requirements: [REF-01, REF-02]
user_setup: []

must_haves:
  truths:
    - "Checkout handles zero-item and coupon-invalid states gracefully"
    - "Store wizard is responsive and provides feedback during geocoding"
  artifacts:
    - path: "src/components/ui/error-boundary.tsx"
      provides: "Component-level error catching"
  key_links: []
---

<objective>
Refine critical UI/UX paths (Checkout and Store Wizard) for a production-ready feel.

Purpose: Improve user trust and system robustness.
Output: Enhanced UI components with better error handling and responsiveness.
</objective>

<execution_context>
@~\.config\opencode\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@src/app/(shop)/checkout/page.tsx
@src/components/store/store-wizard.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Audit and Enhance Checkout UX</name>
  <files>src/app/(shop)/checkout/page.tsx</files>
  <action>
    - Add empty cart state handling with "Continue Shopping" button.
    - Improve coupon application UI: show success/error feedback clearly.
    - Add loading states to the "Proceed to Payment" button.
    - Ensure price breakdown (Subtotal, Discount, Shipping, Total) is responsive.
  </action>
  <verify>Check for new UI elements in the code and conditional rendering for empty states</verify>
  <done>Checkout page is more robust and user-friendly</done>
</task>

<task type="auto">
  <name>task 2: Enhance Store Wizard UX</name>
  <files>src/components/store/store-wizard.tsx</files>
  <action>
    - Add "Searching address..." loading state during geocoding.
    - Improve map visibility on small screens (adjust height).
    - Add validation feedback for all required fields.
    - Ensure final submission shows a clear success/onboarding state.
  </action>
  <verify>Check for state variables tracking geocoding status and improved responsive styles</verify>
  <done>Store Wizard is more professional and resilient</done>
</task>

<task type="auto">
  <name>task 3: Implement Global Error Boundary for Critical Pages</name>
  <files>src/components/ui/error-boundary.tsx, src/app/(shop)/checkout/layout.tsx</files>
  <action>
    - Create a generic `ErrorBoundary` component using React Class syntax (required for error boundaries).
    - Wrap the Checkout and Dashboard layouts with this boundary to prevent full-page crashes.
  </action>
  <verify>Verify if the component is correctly implemented and used</verify>
  <done>Critical flows are protected by error boundaries</done>
</task>

</tasks>

<verification>
Simulate a cart error or geocoding failure to see error states.
Test the layouts on mobile view in the browser.
</verification>

<success_criteria>
Checkout and Store Wizard show no layout breakage on mobile.
System handles common errors (empty cart, invalid coupon) without crashing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-refinement/07-02-SUMMARY.md`
</output>
