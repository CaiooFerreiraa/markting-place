---
phase: 04-checkout-delivery
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/store/use-cart-store.ts, src/components/cart/cart-sidebar.tsx, src/components/cart/cart-item-card.tsx]
autonomous: true
requirements: [CHEC-01, CHEC-02]
user_setup: []

must_haves:
  truths:
    - "Cart items persist in localStorage after page refresh"
    - "Cart UI displays items grouped by store"
    - "Total quantity and price are accurately displayed in real-time"
    - "Buyer can add, remove, and adjust quantities"
  artifacts:
    - path: "src/store/use-cart-store.ts"
      provides: "Zustand cart state with persistence"
      contains: ["persist", "addItem", "removeItem", "updateQuantity"]
    - path: "src/components/cart/cart-sidebar.tsx"
      provides: "Visual cart summary"
      contains: ["Sheet", "ScrollArea"]
  key_links:
    - from: "src/components/cart/cart-sidebar.tsx"
      to: "src/store/use-cart-store.ts"
      via: "Zustand hook selector"
      pattern: "useCartStore\\("
---

<objective>
Implement the global shopping cart with persistence and grouped UI.

Purpose: Provide a seamless shopping experience where buyers can manage items from multiple stores before checkout.
Output: Zustand cart store and Sidebar UI component.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-checkout-delivery/04-CONTEXT.md
@.planning/phases/04-checkout-delivery/04-RESEARCH.md
@.planning/phases/04-checkout-delivery/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Persistent Cart Store</name>
  <files>src/store/use-cart-store.ts</files>
  <action>
    Implement `useCartStore` using `zustand` and `persist` middleware:
    - `items`: `CartItem[]` (containing `productId`, `storeId`, `name`, `price`, `quantity`, `image`).
    - `addItem(product, storeId)`: Appends or increments quantity.
    - `removeItem(productId)`: Filters out item.
    - `updateQuantity(productId, quantity)`: Sets exact quantity.
    - `clearCart()`: Empties items.
    - Computed properties: `totalPrice`, `totalQuantity`, `itemsByStore`.
  </action>
  <verify>Interactive check: add item, refresh page, item remains in cart.</verify>
  <done>Shopping cart state is managed centrally and survives browser reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Build Cart Sidebar UI</name>
  <files>src/components/cart/cart-sidebar.tsx, src/components/cart/cart-item-card.tsx</files>
  <action>
    Build the Cart UI using shadcn/ui Sheet:
    - List items grouped by their respective `storeId` (fetch store names if needed, otherwise include in cart object).
    - Add buttons for quantity adjustment (+/-) and removal.
    - Display subtotal per store and grand total.
    - Link to "Checkout" page.
  </action>
  <verify>Visual check: cart sidebar opens and displays items with grouping.</verify>
  <done>Buyers have a clear, interactive summary of their selected items.</done>
</task>

</tasks>

<verification>
Verify persistence in browser LocalStorage. Check grouping logic with items from 2+ stores.
</verification>

<success_criteria>
- Cart persists across sessions.
- Multi-store grouping is visible in UI.
- CRUD operations for cart items function without state bugs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-delivery/04-02-SUMMARY.md`
</output>
