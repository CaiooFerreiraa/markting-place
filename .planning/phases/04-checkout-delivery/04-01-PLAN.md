---
phase: 04-checkout-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/types/order.ts, src/types/cart.ts]
autonomous: true
requirements: [CHEC-01, CHEC-08]
user_setup: []

must_haves:
  truths:
    - "Prisma schema supports Split Orders (Order -> StoreOrder -> OrderItem)"
    - "TypeScript types exist for the cart and order lifecycle"
    - "OrderStatus enum includes Pending, Paid, Shipped, Delivered, PickedUp, Canceled"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Order and StoreOrder models"
      contains: ["model Order", "model StoreOrder", "model OrderItem"]
    - path: "src/types/order.ts"
      provides: "Fulfillment types and Order states"
    - path: "src/types/cart.ts"
      provides: "Cart structure with store-grouping awareness"
  key_links:
    - from: "Order"
      to: "StoreOrder"
      via: "One-to-many relationship"
    - from: "StoreOrder"
      to: "Store"
      via: "Reference to vendor"
---

<objective>
Update the database schema and type system to support the checkout and delivery lifecycle.

Purpose: Establish the data structure for multi-store orders and fulfillment tracking.
Output: Updated Prisma models and core domain types.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-checkout-delivery/04-CONTEXT.md
@.planning/phases/04-checkout-delivery/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema for Orders</name>
  <files>prisma/schema.prisma</files>
  <action>
    Modify `prisma/schema.prisma` to add:
    - `Order` model: Parent container for a transaction. Fields: `userId`, `totalAmount`, `status` (Enum), `paymentStatus` (Enum), `createdAt`, `updatedAt`.
    - `StoreOrder` model: Fulfillment unit per store. Fields: `orderId`, `storeId`, `status` (Enum), `fulfillmentType` (PICKUP or DELIVERY), `subTotal`, `shippingFee`.
    - `OrderItem` model: Individual product line items. Fields: `storeOrderId`, `productId`, `quantity`, `priceAtPurchase`.
    - `ShippingAddress` model: Linked to `Order`.
    - `OrderStatus` and `FulfillmentType` enums.
    Run `npx prisma generate` after modification.
  </action>
  <verify>npx prisma validate</verify>
  <done>Database schema supports hierarchical orders split by store for multi-vendor fulfillment.</done>
</task>

<task type="auto">
  <name>Task 2: Define Cart and Order Domain Types</name>
  <files>src/types/order.ts, src/types/cart.ts</files>
  <action>
    Create TypeScript definitions:
    - `src/types/cart.ts`: `CartItem` (with store context), `CartState` (total quantity, total price, items grouped by store).
    - `src/types/order.ts`: `OrderWithDetails` (including sub-orders and items), `FulfillmentStatus` mappings.
    Ensure types are exported for the application layer.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Core domain types are available for the cart store and order processing logic.</done>
</task>

</tasks>

<verification>
Check `prisma/schema.prisma` for relations and run `npx prisma validate`.
</verification>

<success_criteria>
- Hierarchical order structure is correctly implemented in Prisma.
- Type system accurately represents the fulfillment lifecycle.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-delivery/04-01-SUMMARY.md`
</output>
