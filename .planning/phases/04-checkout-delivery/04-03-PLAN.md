---
phase: 04-checkout-delivery
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/app/(shop)/checkout/page.tsx, src/components/checkout/fulfillment-selector.tsx, src/components/checkout/checkout-summary.tsx, src/app/api/checkout/create-order/route.ts]
autonomous: false
requirements: [CHEC-03, CHEC-04, CHEC-05, CHEC-06, CHEC-07, CHEC-08]
user_setup: []

must_haves:
  truths:
    - "Buyer can choose fulfillment type (Delivery or Pickup) per store"
    - "Checkout total is re-calculated on the server"
    - "Order is correctly split into StoreOrders based on cart items"
    - "Success page appears after order creation"
  artifacts:
    - path: "src/app/(shop)/checkout/page.tsx"
      provides: "Main checkout workflow"
      contains: ["fulfillmentType", "setFulfillmentType"]
    - path: "src/app/api/checkout/create-order/route.ts"
      provides: "POST /api/checkout/create-order"
      exports: ["POST"]
  key_links:
    - from: "src/app/(shop)/checkout/page.tsx"
      to: "/api/checkout/create-order"
      via: "fetch POST"
      pattern: "fetch.*api/checkout/create-order"
---

<objective>
Implement the checkout flow and the server-side order creation logic.

Purpose: Provide a flexible fulfillment choice and atomic order creation.
Output: Checkout page, Fulfillment UI, and CreateOrder API.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-checkout-delivery/04-CONTEXT.md
@.planning/phases/04-checkout-delivery/04-RESEARCH.md
@.planning/phases/04-checkout-delivery/04-01-SUMMARY.md
@.planning/phases/04-checkout-delivery/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Order Processing API</name>
  <files>src/app/api/checkout/create-order/route.ts</files>
  <action>
    Create POST endpoint for order creation:
    - Authenticate user.
    - Validate cart items and fulfillment choices.
    - **Re-calculate totals on server** fetching current product prices.
    - Use Prisma transaction:
      - Create `Order`.
      - Create `StoreOrder` for each unique store in the cart.
      - Create `OrderItem` for each product.
      - (Optional) Update product stock.
    - Clear server-side cart/session and return 201 with `orderId`.
  </action>
  <verify>POST /api/checkout/create-order with test payload creates Order/StoreOrder/OrderItem rows.</verify>
  <done>Backend correctly splits multi-store carts into manageable sub-orders.</done>
</task>

<task type="auto">
  <name>Task 2: Build Checkout UI and Fulfillment Picker</name>
  <files>src/app/(shop)/checkout/page.tsx, src/components/checkout/fulfillment-selector.tsx, src/components/checkout/checkout-summary.tsx</files>
  <action>
    Implement the checkout page:
    - For each store in the cart:
      - Radio group: "Delivery" or "Pickup at Store".
      - If "Delivery": Form for shipping address.
      - If "Pickup": Show store location and hours.
    - Summary section: Subtotal per store, fees, and grand total.
    - "Complete Purchase" button calling the Order API.
  </action>
  <verify>Visual check: user can switch fulfillment and see totals update.</verify>
  <done>Checkout flow is functional and handles multi-vendor logistics choices.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Checkout Flow</what-built>
  <how-to-verify>
    1. Add items from 2 different stores to cart.
    2. Go to /checkout.
    3. Choose "Delivery" for store A, "Pickup" for store B.
    4. Fill in address for A.
    5. Click "Complete Purchase".
    6. Verify in database: 1 parent Order, 2 StoreOrders (one for A, one for B), correct total price.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check `Prisma` logs for transaction success. Verify `totalAmount` in DB matches re-calculation.
</verification>

<success_criteria>
- Checkout correctly splits multi-store orders.
- Fulfillment choices are correctly persisted in `StoreOrder`.
- Totals match server-side prices, not client-side values.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-delivery/04-03-SUMMARY.md`
</output>
