---
phase: 04-checkout-delivery
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified: [src/app/(shop)/order/[id]/page.tsx, src/components/order/order-details.tsx, src/components/order/order-map.tsx, src/components/order/nav-links.tsx]
autonomous: true
requirements: [CHEC-04, CHEC-05, CHEC-06]
user_setup: []

must_haves:
  truths:
    - "Buyer can see the status of their order (Pending, etc.)"
    - "Pickup orders display a map with the store location"
    - "Buttons exist to open 'Waze' and 'Google Maps' with store coordinates"
  artifacts:
    - path: "src/components/order/order-map.tsx"
      provides: "Map with store markers for pickup"
    - path: "src/components/order/nav-links.tsx"
      provides: "Deep links for Waze/Google Maps"
  key_links:
    - from: "src/app/(shop)/order/[id]/page.tsx"
      to: "src/components/order/nav-links.tsx"
      via: "React Props with coordinates"
---

<objective>
Implement the order confirmation and tracking UI with map routing.

Purpose: Guide the buyer during the post-purchase fulfillment lifecycle, specifically for in-store pickups.
Output: Order page and Navigation/Map components.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-checkout-delivery/04-RESEARCH.md
@.planning/phases/04-checkout-delivery/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order Confirmation & Details Page</name>
  <files>src/app/(shop)/order/[id]/page.tsx, src/components/order/order-details.tsx</files>
  <action>
    Implement the post-order landing page:
    - Fetch `Order` with `StoreOrder` and `OrderItem` details.
    - Show order number, status, and summary.
    - If any `StoreOrder` is a `PICKUP`:
      - Show the store name and full address.
      - Show operating hours.
  </action>
  <verify>Visual check: /order/[id] displays summary and sub-order statuses.</verify>
  <done>Buyers can track the progress of their purchases across different stores.</done>
</task>

<task type="auto">
  <name>Task 2: Maps and Navigation Links</name>
  <files>src/components/order/order-map.tsx, src/components/order/nav-links.tsx</files>
  <action>
    Add routing capabilities:
    - `OrderMap`: Use `react-leaflet` to show store locations (markers) for pickup orders.
    - `NavLinks`: Buttons generating deep links for Waze, Google Maps, and Apple Maps using coordinates from the `Store` model.
  </action>
  <verify>Clicking 'Open in Google Maps' correctly points to the store's lat/lng.</verify>
  <done>Buyers have easy, one-click access to navigation for store pickups.</done>
</task>

</tasks>

<verification>
Check navigation link URL patterns against standard schemes. Verify map markers for 2+ pickup locations.
</verification>

<success_criteria>
- Pickup orders display clear location info.
- Deep links for Waze/Google Maps are functional.
- Map displays all relevant store markers for the order.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-delivery/04-04-SUMMARY.md`
</output>
