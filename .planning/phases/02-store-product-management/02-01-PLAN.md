---
phase: 02-store-product-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/types/store.ts, src/types/product.ts]
autonomous: true
requirements: [STOR-01, PROD-01, PROD-05, PROD-09]
user_setup: []

must_haves:
  truths:
    - "Database schema supports multiple stores per seller"
    - "Products have both retail and wholesale price fields"
    - "Product schema includes inventory and notification fields"
    - "Store schema includes full address and operating hours"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Data models for Phase 2"
      contains: ["model Store", "model Product", "model ProductSubscription"]
    - path: "src/types/store.ts"
      provides: "TypeScript types for Store"
    - path: "src/types/product.ts"
      provides: "TypeScript types for Product"
  key_links:
    - from: "User"
      to: "Store"
      via: "One-to-many relationship"
    - from: "Store"
      to: "Product"
      via: "One-to-many relationship"
---

<objective>
Update the database schema to support the core entities of Phase 2: Stores, Products, and Back-in-stock notifications.

Purpose: Establish the data foundation for multi-store management and complex product catalogs.
Output: Updated Prisma schema and TypeScript definitions.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-store-product-management/02-CONTEXT.md
@.planning/phases/02-store-product-management/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Modify `prisma/schema.prisma` to add:
    - `Store` model: Linked to `User` (1:N). Fields: `name`, `description`, `slug` (unique), `address` (Street, Number, Complement, District, City, State, Zip), `lat` (Float), `lng` (Float), `phone`, `email`, `operatingHours` (JSON), `exceptions` (JSON).
    - `Product` model: Linked to `Store` (1:N). Fields: `name`, `description`, `priceRetail` (Decimal), `priceWholesale` (Decimal?), `minWholesaleQty` (Int?), `stock` (Int), `images` (String[]), `categoryId` (String).
    - `Category` model: Global categories. Fields: `name`, `slug` (unique).
    - `ProductSubscription` model: For back-in-stock notifications. Fields: `email`, `productId`, `notified` (Boolean).
    - Update `User` model: Add `stores Store[]` relationship.
    Run `npx prisma generate` after modification.
  </action>
  <verify>npx prisma validate</verify>
  <done>Prisma schema contains all requested models and relationships with correct field types (Decimal for prices, JSON for hours).</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript Definitions</name>
  <files>src/types/store.ts, src/types/product.ts</files>
  <action>
    Create TypeScript files mirroring the Prisma models and adding domain-specific types:
    - `src/types/store.ts`: `Store` interface, `OperatingHours` (day of week to intervals mapping).
    - `src/types/product.ts`: `Product` interface, `PriceLogic` helper types.
    Ensure types are exported for use in components and APIs.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>TypeScript types correctly represent the domain objects and are type-safe.</done>
</task>

</tasks>

<verification>
Check `prisma/schema.prisma` for consistency and run `npx prisma validate`.
</verification>

<success_criteria>
- `npx prisma validate` passes.
- `npx prisma generate` succeeds.
- TypeScript types match schema definitions exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-store-product-management/02-01-SUMMARY.md`
</output>
