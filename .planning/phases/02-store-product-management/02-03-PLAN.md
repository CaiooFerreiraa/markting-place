---
phase: 02-store-product-management
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/(dashboard)/seller/products/new/page.tsx, src/components/product/product-wizard.tsx, src/app/api/products/route.ts, src/app/api/products/upload/route.ts]
autonomous: false
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07, PROD-08]
user_setup: []

must_haves:
  truths:
    - "Product creation flow has at least 3 steps (Info, Pricing, Images)"
    - "Images are automatically cropped 1:1 using Sharp"
    - "Primary image (first) is correctly flagged/displayed"
    - "Wholesale logic correctly links Price to Min Qty"
  artifacts:
    - path: "src/components/product/product-wizard.tsx"
      provides: "Multi-step product catalog UI"
    - path: "src/app/api/products/upload/route.ts"
      provides: "POST /api/products/upload with Sharp processing"
      contains: ["sharp", "resize(1000, 1000)"]
  key_links:
    - from: "src/components/product/product-wizard.tsx"
      to: "/api/products/upload"
      via: "multipart/form-data POST"
      pattern: "fetch.*api/products/upload"
---

<objective>
Implement product management with image processing and wholesale pricing logic.

Purpose: Provide sellers with tools for creating complex product listings with consistent visuals.
Output: Product creation wizard, image processing service, and product catalog API.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-store-product-management/02-CONTEXT.md
@.planning/phases/02-store-product-management/02-RESEARCH.md
@.planning/phases/02-store-product-management/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Image Processing API (Sharp)</name>
  <files>src/app/api/products/upload/route.ts</files>
  <action>
    Create upload endpoint:
    - Accept multiple files (up to 8).
    - Use `sharp` to:
      - Resize to 1000x1000 (fit: cover, position: center).
      - Convert to WebP (quality: 80).
    - Save processed images to local disk (`public/uploads/products/`) for MVP.
    - Return array of file paths.
  </action>
  <verify>POST /api/products/upload with image results in 1000x1000 WebP in public/uploads</verify>
  <done>Images are correctly processed and saved as uniform square WebP files.</done>
</task>

<task type="auto">
  <name>Task 2: Product Creation Wizard</name>
  <files>src/components/product/product-wizard.tsx, src/app/(dashboard)/seller/products/new/page.tsx, src/app/api/products/route.ts</files>
  <action>
    Implement Product creation flow:
    - Step 1: Info (Name, Description, Category - use fixed set).
    - Step 2: Pricing & Inventory (Price Retail, Price Wholesale, Min Wholesale Qty, Stock).
    - Step 3: Images (File selector, max 8).
    POST /api/products API to link product to selected store.
  </action>
  <verify>Interactive check: user can upload 8 images and set wholesale price.</verify>
  <done>Product creation wizard is fully functional and persists data correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Product Management with Image Processing</what-built>
  <how-to-verify>
    1. Login as Seller.
    2. Go to /seller/products/new.
    3. Upload a non-square image.
    4. Fill all data including wholesale price (min qty = 10).
    5. Click Save.
    6. Verify product appears in catalog.
    7. Inspect image URL in browser to confirm it's 1000x1000 and square.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check `public/uploads/products/` for processed files. Verify Decimal precision in DB.
</verification>

<success_criteria>
- Images are automatically cropped and squared.
- Wholesale pricing logic correctly stores optional fields.
- Categories from the admin-defined list are assigned properly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-store-product-management/02-03-SUMMARY.md`
</output>
