---
phase: 02-store-product-management
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/(dashboard)/seller/stores/new/page.tsx, src/components/store/store-wizard.tsx, src/app/api/stores/route.ts]
autonomous: false
requirements: [STOR-01, STOR-04, STOR-05]
user_setup: []

must_haves:
  truths:
    - "Sellers can navigate to a multi-step store creation form"
    - "Store address is saved with manual fields and coordinates"
    - "Operating hours can be configured per day"
  artifacts:
    - path: "src/components/store/store-wizard.tsx"
      provides: "Multi-step store registration UI"
      contains: ["step", "setStep", "StoreData"]
    - path: "src/app/api/stores/route.ts"
      provides: "POST /api/stores endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/components/store/store-wizard.tsx"
      to: "/api/stores"
      via: "fetch in submitStep"
      pattern: "fetch.*api/stores"
---

<objective>
Implement the store creation wizard and backend API for multi-store management.

Purpose: Allow sellers to register physical store locations with full details and map placement.
Output: Store creation wizard and POST /api/stores endpoint.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-store-product-management/02-CONTEXT.md
@.planning/phases/02-store-product-management/02-RESEARCH.md
@.planning/phases/02-store-product-management/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Store API</name>
  <files>src/app/api/stores/route.ts</files>
  <action>
    Create POST endpoint for store creation:
    - Validate seller authentication.
    - Validate body using Zod: `name`, `description`, `slug`, `address` (Full object), `lat`/`lng` (optional), `phone`, `email`, `operatingHours`, `exceptions`.
    - Handle slug collisions (unique constraint).
    - Save to DB linked to current user.
    - Returns 201 on success.
  </action>
  <verify>curl -X POST /api/stores with test data returns 401/201 based on auth</verify>
  <done>API successfully creates store records in the database with all required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Multi-step Store Wizard</name>
  <files>src/components/store/store-wizard.tsx, src/app/(dashboard)/seller/stores/new/page.tsx</files>
  <action>
    Build the Wizard component:
    - Step 1: Basic Info (Name, Description, Slug).
    - Step 2: Location (Manual address fields + Leaflet Map for pin adjustment).
    - Step 3: Contact & Hours (Phone, Email, Hours editor).
    Use `nuqs` for URL-based step persistence.
    Use `react-leaflet` with dynamic import (ssr: false) for the map.
    The map should update its center based on address input (best effort) and allow manual pin dragging to update `lat`/`lng`.
  </action>
  <verify>Interactive check: user can advance through steps and see map.</verify>
  <done>Wizard UI allows full data entry and communicates correctly with the API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Store Creation Wizard with Leaflet Map</what-built>
  <how-to-verify>
    1. Login as Seller.
    2. Go to /seller/stores/new.
    3. Fill Step 1 (Basic Info).
    4. Fill Step 2 (Location) - Try dragging the map pin.
    5. Fill Step 3 (Contact/Hours).
    6. Click Submit.
    7. Verify store exists in DB (or list if implemented).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check browser console for hydration errors (Leaflet). Verify API response codes.
</verification>

<success_criteria>
- Store creation flow completes without errors.
- Coordinates are captured from the map pin.
- JSON operating hours are stored correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-store-product-management/02-02-SUMMARY.md`
</output>
