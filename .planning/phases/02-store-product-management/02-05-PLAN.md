---
phase: 02-store-product-management
plan: "05"
type: execute
wave: 3
depends_on: ["01", "03"]
files_modified: [src/app/api/products/notify/route.ts, src/components/product/stock-notifier.tsx]
autonomous: true
requirements: [PROD-09]
user_setup: []

must_haves:
  truths:
    - "Users can submit their email for back-in-stock alerts"
    - "Subscriptions are stored in the database"
    - "Notified status is correctly tracked"
  artifacts:
    - path: "src/app/api/products/notify/route.ts"
      provides: "POST /api/products/notify endpoint"
      exports: ["POST"]
    - path: "src/components/product/stock-notifier.tsx"
      provides: "Subscription UI for out-of-stock items"
  key_links:
    - from: "src/components/product/stock-notifier.tsx"
      to: "/api/products/notify"
      via: "fetch POST"
      pattern: "fetch.*api/products/notify"
---

<objective>
Implement the backend and UI for back-in-stock notifications.

Purpose: Allow buyers to register interest in out-of-stock items, capturing future sales.
Output: "Notify Me" API and frontend component.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-store-product-management/02-CONTEXT.md
@.planning/phases/02-store-product-management/02-RESEARCH.md
@.planning/phases/02-store-product-management/02-01-SUMMARY.md
@.planning/phases/02-store-product-management/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back-in-stock API & Component</name>
  <files>src/app/api/products/notify/route.ts, src/components/product/stock-notifier.tsx</files>
  <action>
    Implement notification registration:
    - API: `POST /api/products/notify` accepting `{ email, productId }`.
    - Upsert to `ProductSubscription` (avoid duplicate notifications for same email/product).
    - Frontend Component: `StockNotifier` - A simple form (Email field + "Notify Me" button).
    - Display `StockNotifier` on the dashboard or store page when a product has `stock: 0`.
  </action>
  <verify>POST /api/products/notify returns 200/201 and stores record in DB.</verify>
  <done>Buyers can successfully register for stock notifications.</done>
</task>

</tasks>

<verification>
Check `ProductSubscription` table for test records. Verify email validation on API.
</verification>

<success_criteria>
- Users can register for notifications without being logged in.
- Duplicate subscriptions are handled gracefully (200 OK, no duplicate record).
- Record contains `productId` correctly linked.
</success_criteria>

<output>
After completion, create `.planning/phases/02-store-product-management/02-05-SUMMARY.md`
</output>
