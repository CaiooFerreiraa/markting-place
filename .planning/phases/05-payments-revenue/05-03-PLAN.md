---
phase: 05-payments-revenue
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/app/api/checkout/create-stripe-session/route.ts, src/app/api/webhooks/stripe/route.ts]
autonomous: true
requirements: [PAYM-01, PAYM-04, PAYM-05]
user_setup:
  - service: stripe
    why: "Checkout payment processing & order updates"
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe CLI or Stripe Dashboard -> Developers -> Webhooks"
    dashboard_config:
      - task: "Register webhook endpoint for checkout events"
        location: "Stripe Dashboard -> Developers -> Webhooks"

must_haves:
  truths:
    - "Checkout session splits funds to seller and platform"
    - "Order status updates to PAID upon webhook receipt"
  artifacts:
    - path: "src/app/api/checkout/create-stripe-session/route.ts"
      provides: "Checkout integration"
      contains: ["application_fee_amount", "transfer_data"]
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Status synchronization"
      contains: ["checkout.session.completed", "updateMany"]
---

<objective>
Implement the integrated checkout with Stripe and webhook status updates.

Purpose: Allow buyers to pay for their orders and automatically update the order status to PAID.
Output: Stripe session creation API and Webhook handler.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-payments-revenue/05-RESEARCH.md
@.planning/phases/05-payments-revenue/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Stripe Session Creator API</name>
  <files>src/app/api/checkout/create-stripe-session/route.ts</files>
  <action>
    Implement POST handler:
    - Receive an `orderId`.
    - Fetch order with items and store information.
    - Create a Stripe Checkout Session with `destination` charges:
      - Transfer to each `store.user.stripeAccountId`.
      - Calculate platform commission (PAYM-01) as `application_fee_amount`.
    - Use `payment_intent_data` for fund splits.
    - Return the session URL.
  </action>
  <verify>curl -X POST /api/checkout/create-stripe-session returns a Stripe URL for a valid order.</verify>
  <done>Buyers can transition from order creation to payment.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Stripe Webhook Handler</name>
  <files>src/app/api/webhooks/stripe/route.ts</files>
  <action>
    Implement POST handler for Stripe webhooks:
    - Verify Stripe signature using `STRIPE_WEBHOOK_SECRET`.
    - Listen for `checkout.session.completed`.
    - Retrieve `orderId` from metadata.
    - Update `Order` status to PAID and `PaymentStatus` to PAID.
    - Update all child `StoreOrder` status to PAID.
  </action>
  <verify>Use Stripe CLI to trigger 'checkout.session.completed' and check DB order status.</verify>
  <done>Database state is automatically synchronized with Stripe payment results.</done>
</task>

</tasks>

<verification>
Test session creation and webhook processing using mock Stripe CLI data.
</verification>

<success_criteria>
- Stripe Checkout session is created with correct line items and amounts.
- Webhook signature is verified before processing any database changes.
- Order and sub-order statuses are correctly updated upon successful payment.
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-revenue/05-03-SUMMARY.md`
</output>
