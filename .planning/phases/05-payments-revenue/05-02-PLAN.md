---
phase: 05-payments-revenue
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/api/stripe/onboard/route.ts, src/components/dashboard/seller/payment-setup.tsx]
autonomous: true
requirements: [PAYM-01, PAYM-03]
user_setup:
  - service: stripe
    why: "Marketplace seller onboarding"
    dashboard_config:
      - task: "Configure platform branding for Connect"
        location: "Stripe Dashboard -> Settings -> Connect -> Branding"

must_haves:
  truths:
    - "Sellers can generate onboarding links"
    - "Dashboard shows accurate verification status"
  artifacts:
    - path: "src/app/api/stripe/onboard/route.ts"
      provides: "Onboarding redirect logic"
      contains: ["account_links.create"]
    - path: "src/components/dashboard/seller/payment-setup.tsx"
      provides: "Stripe connection UI"
      contains: ["charges_enabled"]
---

<objective>
Implement the seller onboarding flow for Stripe Connect Express.

Purpose: Allow sellers to register with Stripe to receive payments for their store sales.
Output: Onboarding API route and Dashboard setup UI.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-payments-revenue/05-RESEARCH.md
@.planning/phases/05-payments-revenue/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Onboarding API Route</name>
  <files>src/app/api/stripe/onboard/route.ts</files>
  <action>
    Implement POST handler:
    - Check if seller already has `stripeAccountId`. If not, create one (type: 'express', country: 'BR').
    - Create an `account_link` (type: 'account_onboarding') for the Stripe account.
    - Return the account link URL for redirection.
    - Set `refresh_url` and `return_url` back to the dashboard.
  </action>
  <verify>curl -X POST /api/stripe/onboard returns a JSON object with a Stripe URL.</verify>
  <done>Sellers can generate links to complete their Stripe KYC.</done>
</task>

<task type="auto">
  <name>Task 2: Build Payment Setup Dashboard Component</name>
  <files>src/components/dashboard/seller/payment-setup.tsx</files>
  <action>
    Create a component for the Seller Dashboard:
    - Check if Stripe account is active and verified (`charges_enabled`).
    - If not verified, show a "Connect with Stripe" button.
    - Button triggers the onboarding API and redirects the user.
    - Show onboarding status (Pending/Verified).
  </action>
  <verify>Visual check: component shows correct onboarding state based on DB/Stripe flags.</verify>
  <done>Sellers have a clear UI path to set up their payout account.</done>
</task>

</tasks>

<verification>
Check onboarding link generation and dashboard state updates.
</verification>

<success_criteria>
- Sellers can click a button to initiate Stripe onboarding.
- Stripe account IDs are correctly persisted to the database.
- Sellers are redirected back to the dashboard after Stripe setup.
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-revenue/05-02-SUMMARY.md`
</output>
