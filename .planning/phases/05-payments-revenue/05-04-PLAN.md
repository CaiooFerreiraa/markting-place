---
phase: 05-payments-revenue
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified: [src/app/api/stripe/subscribe/route.ts, src/components/dashboard/seller/subscription-card.tsx]
autonomous: true
requirements: [PAYM-02]
user_setup:
  - service: stripe
    why: "Marketplace seller subscriptions"
    dashboard_config:
      - task: "Create monthly subscription product and price"
        location: "Stripe Dashboard -> Products -> Create Product"

must_haves:
  truths:
    - "Sellers can initiate subscription checkout"
    - "Subscription state is reflected in the seller dashboard"
  artifacts:
    - path: "src/app/api/stripe/subscribe/route.ts"
      provides: "Subscription sessions"
      contains: ["mode: 'subscription'"]
    - path: "src/components/dashboard/seller/subscription-card.tsx"
      provides: "Billing UI"
      contains: ["subscriptionStatus"]
---

<objective>
Implement the monthly subscription model for sellers.

Purpose: Provide an alternative revenue model where sellers pay a fixed monthly fee for platform access.
Output: Subscription API and Seller Dashboard UI for billing.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-payments-revenue/05-RESEARCH.md
@.planning/phases/05-payments-revenue/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Subscription API Route</name>
  <files>src/app/api/stripe/subscribe/route.ts</files>
  <action>
    Implement POST handler for subscription sessions:
    - Create a Stripe `Customer` if not present in the `User` record.
    - Create a Stripe Checkout Session in `subscription` mode.
    - Use a price ID from environment variables (`STRIPE_MONTHLY_PRICE_ID`).
    - Redirect back to the seller dashboard.
    - Webhook will handle `invoice.paid` to update `subscriptionStatus`.
  </action>
  <verify>curl -X POST /api/stripe/subscribe returns a subscription checkout URL.</verify>
  <done>Sellers can subscribe to the platform monthly plan.</done>
</task>

<task type="auto">
  <name>Task 2: Build Subscription Dashboard UI</name>
  <files>src/components/dashboard/seller/subscription-card.tsx</files>
  <action>
    Create a component for the Seller Dashboard:
    - Display current subscription status (Active/Inactive).
    - Provide a "Subscribe Now" button if inactive.
    - Show plan details (Monthly price).
  </action>
  <verify>Visual check: component correctly shows subscription status based on database state.</verify>
  <done>Sellers have a clear way to manage their platform access fee.</done>
</task>

</tasks>

<verification>
Check subscription session creation and dashboard display logic.
</verification>

<success_criteria>
- Sellers can initiate monthly subscription checkout.
- Subscription status is correctly reflected in the database and UI.
- Webhooks correctly update subscription state on payment success or failure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-revenue/05-04-SUMMARY.md`
</output>
