---
phase: 05-payments-revenue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/lib/stripe.ts]
autonomous: true
requirements: [PAYM-01, PAYM-02, PAYM-03]
user_setup:
  - service: stripe
    why: "Marketplace payment processing & seller subscriptions"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys"

must_haves:
  truths:
    - "User model includes Stripe account and customer references"
    - "Stripe client singleton is available in the application"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Stripe fields in User model"
      contains: ["stripeAccountId", "stripeCustomerId", "subscriptionStatus"]
    - path: "src/lib/stripe.ts"
      provides: "Stripe SDK client"
      contains: ["new Stripe"]
---

<objective>
Initialize the payment infrastructure by updating the database schema and setting up the Stripe client.

Purpose: Prepare the system to store Stripe-related metadata and communicate with the Stripe API.
Output: Updated Prisma schema and Stripe client utility.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-payments-revenue/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema with Stripe Fields</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add the following fields to the `User` model:
    - `stripeAccountId`: String? (For Connect Express accounts)
    - `stripeCustomerId`: String? (For Billing/Subscriptions)
    - `subscriptionStatus`: String? (e.g., 'active', 'canceled')
    - `revenueModel`: String @default('TRANSACTION_FEE') (To track PAYM-03 choice)

    Run `npx prisma generate` after modification.
  </action>
  <verify>npx prisma validate</verify>
  <done>Database schema is ready to track Stripe relationships.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Stripe Client</name>
  <files>src/lib/stripe.ts</files>
  <action>
    Create a singleton Stripe client using the `stripe` library.
    Use `process.env.STRIPE_SECRET_KEY` and set `apiVersion` to '2025-01-27.acacia' (or latest stable).
  </action>
  <verify>Check if src/lib/stripe.ts exports a valid Stripe instance.</verify>
  <done>Server-side components can interact with Stripe API securely.</done>
</task>

</tasks>

<verification>
Verify prisma schema integrity and stripe client instantiation.
</verification>

<success_criteria>
- Prisma schema includes all required stripe metadata fields.
- Stripe client is correctly initialized with environment variables.
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-revenue/05-01-SUMMARY.md`
</output>
