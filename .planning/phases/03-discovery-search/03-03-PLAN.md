---
phase: 03-discovery-search
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/(shop)/product/[slug]/page.tsx, src/app/sitemap.ts, src/components/seo/json-ld.tsx]
autonomous: true
requirements: [SEAR-03, SEAR-04, SEAR-05]
user_setup: []

must_haves:
  truths:
    - "Dynamic product pages have unique meta tags (title, description)"
    - "Search engines can discover all product URLs via sitemap"
    - "Product data is correctly structured for rich results (JSON-LD)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Automated sitemap generation"
      contains: ["prisma.product.findMany", "sitemap"]
    - path: "src/components/seo/json-ld.tsx"
      provides: "Structured data component"
      contains: ["application/ld+json"]
  key_links:
    - from: "src/app/(shop)/product/[slug]/page.tsx"
      to: "generateMetadata"
      via: "Next.js dynamic metadata function"
      pattern: "export async function generateMetadata"
---

<objective>
Optimize the platform for search engines with dynamic metadata, sitemaps, and structured data.

Purpose: Increase organic traffic by making products discoverable and correctly structured for search engine rich results.
Output: Dynamic SEO tags and automated sitemap.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discovery-search/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic Metadata & Product Pages</name>
  <files>src/app/(shop)/product/[slug]/page.tsx</files>
  <action>
    - Create dynamic product page at `/product/[slug]`.
    - Implement `generateMetadata` function fetching product by slug.
    - Set `title` as `product.name | Marketplace VDC`.
    - Set `description` to `product.description`.
    - Handle 404 if product not found.
  </action>
  <verify>Navigating to a product page sets the browser title to the product's name.</verify>
  <done>Each product has unique SEO meta tags for title and description.</done>
</task>

<task type="auto">
  <name>Task 2: Sitemap & JSON-LD Implementation</name>
  <files>src/app/sitemap.ts, src/components/seo/json-ld.tsx</files>
  <action>
    - Create `src/app/sitemap.ts` following Next.js 15 standards.
    - Fetch all product slugs and updated dates to generate sitemap entries.
    - Create `src/components/seo/json-ld.tsx` for product structured data (`@type: Product`).
    - Integrate `JsonLd` component in `/product/[slug]` page.
  </action>
  <verify>Accessing /sitemap.xml returns a valid XML list of product URLs.</verify>
  <done>Sitemaps are auto-generated and product pages include SEO-friendly JSON-LD structured data.</done>
</task>

</tasks>

<verification>
Verify sitemap.xml structure. Check JSON-LD syntax in browser source.
</verification>

<success_criteria>
- Each product page has unique metadata.
- /sitemap.xml is accessible and contains valid dynamic URLs.
- Product structured data is valid JSON-LD.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-search/03-03-SUMMARY.md`
</output>
