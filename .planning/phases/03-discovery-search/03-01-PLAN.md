---
phase: 03-discovery-search
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/lib/search-params.ts, src/types/discovery.ts]
autonomous: true
requirements: [SEAR-01, SEAR-02, SEAR-03]
user_setup: []

must_haves:
  truths:
    - "Prisma full-text search is enabled in schema"
    - "Search parameters are defined in a central location"
    - "TypeScript types exist for search results and filters"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Full-text search configuration"
      contains: "previewFeatures = [\"fullTextSearchPostgres\"]"
    - path: "src/lib/search-params.ts"
      provides: "URL-state synchronization logic"
      contains: "nuqs"
  key_links:
    - from: "src/lib/search-params.ts"
      to: "nuqs"
      via: "import"
---

<objective>
Enable full-text search in Prisma and define the URL-state synchronization layer.

Purpose: Provide the technical foundation for performant search and shareable filtered views.
Output: Configured Prisma schema and central search parameter definitions.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discovery-search/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Prisma Full-Text Search</name>
  <files>prisma/schema.prisma</files>
  <action>
    - Add `previewFeatures = ["fullTextSearchPostgres"]` to the `generator client` block in `prisma/schema.prisma`.
    - Run `npx prisma generate` to update the client.
  </action>
  <verify>npx prisma validate</verify>
  <done>Prisma client is generated with support for the `search` operator in PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 2: Define Search Parameters with nuqs</name>
  <files>src/lib/search-params.ts, src/types/discovery.ts</files>
  <action>
    - Install `nuqs` if not present.
    - Create `src/lib/search-params.ts` with definitions for: `q` (string), `category` (string), `minPrice` (int), `maxPrice` (int), `sort` (string).
    - Create `src/types/discovery.ts` for search result and filter state types.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Centralized, type-safe search parameters are ready for use in components and pages.</done>
</task>

</tasks>

<verification>
Check `prisma/schema.prisma` for preview feature. Verify `nuqs` definitions in `src/lib/search-params.ts`.
</verification>

<success_criteria>
- Prisma supports `search` in `where` clauses.
- `nuqs` search parameters are correctly typed and exported.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-search/03-01-SUMMARY.md`
</output>
