---
phase: 03-discovery-search
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/(shop)/search/page.tsx, src/components/discovery/search-bar.tsx, src/components/discovery/filter-sidebar.tsx, src/components/discovery/sort-dropdown.tsx]
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-04, FILT-05, SEAR-01, SEAR-02]
user_setup: []

must_haves:
  truths:
    - "Searching by name/description returns relevant products"
    - "Filtering by category and price range updates results dynamically"
    - "Sorting by price (ASC/DESC) and newest works"
    - "Filters and search queries are correctly synced with the URL"
  artifacts:
    - path: "src/app/(shop)/search/page.tsx"
      provides: "Main search results page with Server Components"
      contains: ["prisma.product.findMany", "search"]
    - path: "src/components/discovery/filter-sidebar.tsx"
      provides: "Sidebar for category and price filtering"
      contains: ["useQueryState"]
  key_links:
    - from: "src/app/(shop)/search/page.tsx"
      to: "prisma.product"
      via: "Server Component database query"
      pattern: "prisma\\.product\\.findMany"
---

<objective>
Implement the main search results page with real-time filtering and sorting.

Purpose: Allow buyers to find products efficiently through a centralized search interface.
Output: Search results page with filter sidebar and sorting capabilities.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discovery-search/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Search API Logic</name>
  <files>src/app/(shop)/search/page.tsx</files>
  <action>
    - Create a Server Component for `/search`.
    - Fetch products using `prisma.product.findMany`.
    - Apply `where` clause:
      - `search`: use `fullTextSearch` on name and description if `q` exists.
      - `categoryId`: filter if `category` parameter exists.
      - `priceRetail`: filter by `minPrice` and `maxPrice`.
    - Apply `orderBy` based on `sort` parameter (e.g., `priceRetail: asc/desc`, `createdAt: desc`).
    - Handle empty states (no results).
  </action>
  <verify>Searching for a product by name returns correct results.</verify>
  <done>Search results are dynamically fetched and filtered on the server based on URL parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Filter UI Components</name>
  <files>src/components/discovery/search-bar.tsx, src/components/discovery/filter-sidebar.tsx, src/components/discovery/sort-dropdown.tsx</files>
  <action>
    - `SearchBar`: Debounced input that updates `q` in URL using `useQueryState`.
    - `FilterSidebar`:
      - Category list (fetch from Prisma `Category`).
      - Price range slider/inputs (updates `minPrice`, `maxPrice`).
    - `SortDropdown`: Dropdown for sorting options (Price L-H, Price H-L, Newest).
    All components must use `nuqs` to sync with the URL.
  </action>
  <verify>Updating a filter in the sidebar reflects correctly in the URL search params.</verify>
  <done>Buyers can interactively filter and sort results using intuitive UI components.</done>
</task>

</tasks>

<verification>
Check `search` functionality with multiple keywords. Verify price range filters inclusive/exclusive logic.
</verification>

<success_criteria>
- Search results match keywords using PostgreSQL FTS.
- URL accurately represents current filter/sort state.
- Category filter narrows down results as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-search/03-02-SUMMARY.md`
</output>
