---
phase: 01-authentication-user-profiles
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/(dashboard)/buyer/profile/page.tsx
  - src/app/(dashboard)/seller/dashboard/page.tsx
  - src/app/(dashboard)/seller/profile/page.tsx
  - src/app/(dashboard)/admin/dashboard/page.tsx
  - src/app/api/profile/route.ts
  - src/app/api/profile/upgrade-to-seller/route.ts
  - src/components/profile/buyer-profile-form.tsx
  - src/components/profile/seller-profile-form.tsx
  - src/components/providers/auth-provider.tsx
  - src/middleware.ts
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
must_haves:
  truths:
    - "Buyer can view and edit own profile"
    - "Buyer can upgrade to Seller role"
    - "Seller can access /seller/dashboard"
    - "Seller can manage own store profile (store name)"
    - "Admin can access /admin/dashboard"
    - "Role-based route protection enforced"
  artifacts:
    - path: "src/app/(dashboard)/buyer/profile/page.tsx"
      provides: "Buyer profile page"
    - path: "src/app/(dashboard)/seller/dashboard/page.tsx"
      provides: "Seller dashboard"
    - path: "src/app/(dashboard)/seller/profile/page.tsx"
      provides: "Seller profile page"
    - path: "src/app/(dashboard)/admin/dashboard/page.tsx"
      provides: "Admin dashboard"
    - path: "src/app/api/profile/route.ts"
      provides: "Profile get/update API"
    - path: "src/app/api/profile/upgrade-to-seller/route.ts"
      provides: "Role upgrade API"
  key_links:
    - from: "src/app/(dashboard)/buyer/profile/page.tsx"
      to: "src/app/api/profile/route.ts"
      via: "PATCH request"
    - from: "src/app/(dashboard)/seller/dashboard/page.tsx"
      to: "src/auth.ts"
      via: "session role check"
    - from: "src/middleware.ts"
      to: "src/auth.ts"
      via: "role-based route protection"
---

<objective>
Implement user profiles with role-based dashboards: Buyer profile, Seller dashboard, Admin dashboard, and role upgrade functionality.
</objective>

<context>
@.planning/phases/01-authentication-user-profiles/01-02-SUMMARY.md
@.planning/phases/01-authentication-user-profiles/01-CONTEXT.md

**Key Locked Decisions (from CONTEXT.md):**
- Role selection happens AFTER registration (in profile settings)
- Separate URLs per role: /admin, /seller, /buyer
- Admin accounts created manually in database only (not self-registration)
- Simple upgrade path: user clicks to become seller, then completes store details later (Phase 2)
- Buyer can view and edit own profile (PROF-05)
</context>

<tasks>

<task type="auto">
  <name>Create Buyer profile page</name>
  <files>src/app/(dashboard)/buyer/profile/page.tsx, src/components/profile/buyer-profile-form.tsx</files>
  <action>
Create BuyerProfileForm component:
- Display user info: name, email, role
- Allow editing name (profile update per PROF-05)
- Use React Hook Form + Zod
- Submit to /api/profile

Create Buyer profile page:
- Path: /buyer/profile
- Protected by auth + role check (BUYER only)
- Show user details and edit form
- Show "Become a Seller" button to upgrade role
- Use Card, Form components
</action>
  <verify>Buyer can view and edit their profile at /buyer/profile</verify>
  <done>Buyer profile page displays user info, allows name editing</done>
</task>

<task type="auto">
  <name>Create profile update API</name>
  <files>src/app/api/profile/route.ts</files>
  <action>
Create profile API route:
- GET: Return current user profile (name, email, role, storeName if seller)
- PATCH: Update user profile (name for all roles)
- Authenticate using auth() from @/auth
- Validate input with Zod
- Update user in database
- Return updated user
</action>
  <verify>PATCH /api/profile with auth updates user name in database</verify>
  <done>Profile API handles get and update operations</done>
</task>

<task type="auto">
  <name>Create Seller upgrade functionality</name>
  <files>src/app/api/profile/upgrade-to-seller/route.ts</files>
  <action>
Create upgrade-to-seller API:
- Check current user is authenticated
- Check current role is BUYER
- Update role to SELLER
- Set storeName to null (store details in Phase 2 per CONTEXT.md deferred)
- Return updated user

Update Buyer profile form:
- Add "Become a Seller" button
- On click, call upgrade API
- Show success toast and redirect to /seller/dashboard
</action>
  <verify>Buyer clicking upgrade button becomes SELLER role</verify>
  <done>User can upgrade from Buyer to Seller role</done>
</task>

<task type="auto">
  <name>Create Seller dashboard and profile</name>
  <files>src/app/(dashboard)/seller/dashboard/page.tsx, src/app/(dashboard)/seller/profile/page.tsx, src/components/profile/seller-profile-form.tsx</files>
  <action>
Create Seller dashboard page:
- Path: /seller/dashboard
- Protected by auth + role check (SELLER only)
- Show welcome message with store name
- Show quick stats placeholder (orders, products - Phase 2)
- Show "Complete store profile" link (Phase 2)

Create Seller profile page:
- Path: /seller/profile
- Protected by auth + role check (SELLER only)
- Show/edit store name, contact info
- Store details (address, hours) in Phase 2 per CONTEXT.md
- Use Card, Form components
</action>
  <verify>Seller can access /seller/dashboard and /seller/profile</verify>
  <done>Seller dashboard and profile pages functional</done>
</task>

<task type="auto">
  <name>Create Admin dashboard</name>
  <files>src/app/(dashboard)/admin/dashboard/page.tsx</files>
  <action>
Create Admin dashboard page:
- Path: /admin/dashboard
- Protected by auth + role check (ADMIN only)
- Show platform stats: total users, stores, orders
- List recent signups
- Use Card components for stats
- Note: Admin accounts created manually per CONTEXT.md
</action>
  <verify>Admin can access /admin/dashboard, regular users cannot</verify>
  <done>Admin dashboard accessible only to ADMIN role</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete authentication and profiles flow</what-built>
  <how-to-verify>
    1. Register new user as Buyer at /register
    2. Verify email via link received
    3. Log in at /login - should redirect to home
    4. Visit /buyer/profile - should show profile with name edit
    5. Click "Become a Seller" - role should upgrade to SELLER
    6. Visit /seller/dashboard - should show seller dashboard
    7. Visit /seller/profile - should show seller profile form
    8. Log out via header button - should redirect to home
    9. Check header shows Login/Register when logged out
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Buyer profile page at /buyer/profile
- [ ] User can edit name in profile
- [ ] Buyer can upgrade to Seller
- [ ] Seller dashboard at /seller/dashboard
- [ ] Seller profile at /seller/profile
- [ ] Admin dashboard at /admin/dashboard
- [ ] Role-based route protection works
- [ ] Human verification of complete flow
</verification>

<success_criteria>
User profiles and role management complete:
- Buyer can view/edit profile (PROF-05)
- Buyer can upgrade to Seller (PROF-02)
- Seller can access dashboard (PROF-02)
- Seller can manage store profile (PROF-04) - basic storeName
- Admin can access admin dashboard (PROF-03)
- All role-based routes protected
- All AUTH requirements (AUTH-01 through AUTH-05) verified working
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-user-profiles/{phase}-03-SUMMARY.md`
</output>
