---
phase: 01-authentication-user-profiles
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/api/auth/register/route.ts
  - src/app/api/auth/verify-email/route.ts
  - src/app/api/auth/forgot-password/route.ts
  - src/app/api/auth/reset-password/route.ts
  - src/components/auth/login-form.tsx
  - src/components/auth/register-form.tsx
  - src/components/auth/logout-button.tsx
  - src/components/layout/header.tsx
  - src/components/providers/auth-provider.tsx
  - src/middleware.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - "User can register with email/password"
    - "User receives email verification after signup"
    - "User can log in with credentials or Google"
    - "Session persists across browser refresh (24 hours)"
    - "User can log out from header button"
    - "User can reset password via email link"
    - "Generic error messages shown (no specific field errors)"
  artifacts:
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Registration page"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page"
    - path: "src/app/api/auth/register/route.ts"
      provides: "Registration API"
    - path: "src/components/auth/logout-button.tsx"
      provides: "Logout button for header"
    - path: "src/app/api/auth/verify/route.ts"
      provides: "Email verification"
    - path: "src/app/api/auth/forgot-password/route.ts"
      provides: "Password reset request"
    - path: "src/app/api/auth/reset-password/route.ts"
      provides: "Password reset execution"
  key_links:
    - from: "src/components/auth/register-form.tsx"
      to: "src/app/api/auth/register/route.ts"
      via: "POST fetch"
    - from: "src/components/auth/login-form.tsx"
      to: "src/auth.ts"
      via: "signIn credentials"
    - from: "src/middleware.ts"
      to: "src/auth.ts"
      via: "NextAuth middleware"
---

<objective>
Implement complete authentication flow: registration, login, logout, email verification, and password reset with proper error handling.
</objective>

<context>
@.planning/phases/01-authentication-user-profiles/01-01-SUMMARY.md
@.planning/phases/01-authentication-user-profiles/01-CONTEXT.md

**Key Locked Decisions (from CONTEXT.md):**
- Separate pages for login and registration (not combined form)
- Google login + Email/Password from the start
- Role selection happens AFTER registration (in profile settings)
- Generic error messages (e.g., "Invalid credentials" not "Wrong password")
- Toast notifications for error display
- 24-hour sessions (no "remember me")
- Header button for logout on all authenticated pages
</context>

<tasks>

<task type="auto">
  <name>Create registration page with form</name>
  <files>src/app/(auth)/register/page.tsx, src/components/auth/register-form.tsx</files>
  <action>
Create registration form component (src/components/auth/register-form.tsx):
- Use React Hook Form + Zod validation
- Fields: name, email, password, confirmPassword
- Validation: email format, password min 8 chars, passwords match
- Submit to /api/auth/register
- Show toast errors with generic messages
- On success, redirect to /login with "verify your email" message
- Use Card, Form, Input, Button, Label from shadcn/ui

Create registration page (src/app/(auth)/register/page.tsx):
- Simple layout with centered card
- Heading: "Criar conta"
- Link to login page

Register API route (src/app/api/auth/register/route.ts):
- Validate input with Zod
- Check if email already exists
- Hash password with bcryptjs
- Create user with role BUYER (default per CONTEXT.md - role upgrade happens later)
- Generate verification token and send email via Resend
- Return success/error response
</action>
  <verify>POST to /api/auth/register with valid data returns 201 and creates user</verify>
  <done>Registration form submits to API, creates user with hashed password, sends verification email</done>
</task>

<task type="auto">
  <name>Create login page with form</name>
  <files>src/app/(auth)/login/page.tsx, src/components/auth/login-form.tsx</files>
  <action>
Create login form component (src/components/auth/login-form.tsx):
- Use React Hook Form + Zod validation
- Fields: email, password
- Submit to NextAuth signIn("credentials")
- Use toast for generic error: "Invalid credentials" (per CONTEXT.md)
- On success, redirect to home page (per CONTEXT.md)
- Show "Forgot password?" link
- Show "Sign up" link
- Use Card, Form, Input, Button from shadcn/ui
- Add Google sign-in button using NextAuth signIn("google")

Create login page (src/app/(auth)/login/page.tsx):
- Simple layout with centered card
- Heading: "Entrar"
- Link to register page
</action>
  <verify>Valid credentials redirect to home page, invalid show toast with "Invalid credentials"</verify>
  <done>Login form authenticates user via NextAuth, redirects to home on success</done>
</task>

<task type="auto">
  <name>Create logout functionality</name>
  <files>src/components/auth/logout-button.tsx, src/components/layout/header.tsx</files>
  <action>
Create LogoutButton component (src/components/auth/logout-button.tsx):
- Use NextAuth signOut() with redirect to home page
- Button with logout icon

Update Header component (src/components/layout/header.tsx):
- Check session using auth() from @/auth
- If authenticated: show user name/avatar + LogoutButton
- If not authenticated: show Login and Register links
- This satisfies "Header button for logout on all authenticated pages" per CONTEXT.md
</action>
  <verify>Logout button visible in header when logged in, clicking logs out and redirects to home</verify>
  <done>Logout button in header works on all authenticated pages</done>
</task>

<task type="auto">
  <name>Implement email verification flow</name>
  <files>src/app/api/auth/verify-email/route.ts, src/app/api/auth/verify/route.ts</files>
  <action>
Create verify-email API (src/app/api/auth/verify/route.ts):
- Accept token from query params
- Find VerificationToken in DB
- If valid: update User.emailVerified to now, delete token
- Redirect to /login?verified=true

Resend verification email:
- Add /api/auth/resend-verification route
- Check if user exists and email not verified
- Generate new token, send via Resend
- Rate limit: 1 email per minute
</action>
  <verify>Clicking email link marks email as verified, user can then login</verify>
  <done>Email verification flow completes successfully</done>
</task>

<task type="auto">
  <name>Implement password reset flow</name>
  <files>src/app/(auth)/forgot-password/page.tsx, src/app/(auth)/reset-password/page.tsx, src/app/api/auth/forgot-password/route.ts, src/app/api/auth/reset-password/route.ts</files>
  <action>
Create forgot-password page:
- Simple form with email input
- Submit to /api/auth/forgot-password
- Show "If account exists, you will receive reset email" (generic per CONTEXT.md)

Create reset-password page:
- Accept token from query params
- Form with new password, confirm password
- Submit to /api/auth/reset-password

Create forgot-password API:
- Find user by email
- If exists: generate reset token, send email via Resend
- Always return success (don't reveal if email exists)

Create reset-password API:
- Validate token exists and not expired
- Hash new password with bcryptjs
- Update user password
- Delete used token
- Return success
</action>
  <verify>User can request password reset email and successfully reset password</verify>
  <done>Password reset flow works end-to-end</done>
</task>

<task type="auto">
  <name>Configure middleware for session protection</name>
  <files>src/middleware.ts</files>
  <action>
Create src/middleware.ts:
- Use NextAuth middleware for session protection
- Protect /dashboard/* routes (will be created in Plan 03)
- Allow public routes: /, /login, /register, /forgot-password, /reset-password
- Add static assets to matcher
</action>
  <verify>Middleware configuration exists, protects dashboard routes</verify>
  <done>Middleware configured for session-based route protection</done>
</task>

</tasks>

<verification>
- [ ] Registration form creates user, sends verification email
- [ ] Login form authenticates with credentials or Google
- [ ] Logout button visible in header when authenticated
- [ ] Email verification marks user as verified
- [ ] Password reset flow works end-to-end
- [ ] Middleware protects authenticated routes
</verification>

<success_criteria>
Complete authentication flow working:
- User can register with email/password
- User receives email verification
- User can log in (credentials + Google)
- Session persists across browser refresh (24h)
- User can log out from header
- User can reset password via email
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-user-profiles/{phase}-02-SUMMARY.md`
</output>
